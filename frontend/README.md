# Frontend

React + Vite client for Document Summary Assistant.

```bash
npm install
copy .env.example .env
npm run dev
```

Required env: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `VITE_API_BASE_URL`.
